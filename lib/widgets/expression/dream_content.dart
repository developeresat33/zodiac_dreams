import 'package:flutter/material.dart';

class DreamContent extends StatelessWidget {
  const DreamContent({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [],
    );
  }
}
